import { makeStyles } from "@mui/styles";

export default makeStyles(
  () => ({
    icons: {
      background: "#EFF5F8",
      borderRadius: "10%",
      marginRight: "1em",
      padding: "0.5em"
    }
  }),
  {
    name: "SaveButtonBar"
  }
);
